import { projects } from "../../helpers/projects";

const initialState = {
  projects: projects,
};

export const projectsReducer = (state = initialState, action) => {
  //   switch (key) {
  //     case value:
  //       break;
  //   }

  return state;
};
