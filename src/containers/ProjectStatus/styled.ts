import styled from "styled-components";

import { Row } from "../../components/Layout";

export const ProjectStatusWrapp = styled.div``;

export const ProjectStatusHeader = styled(Row)``;

export const ProjectStatusBody = styled(Row)``;
