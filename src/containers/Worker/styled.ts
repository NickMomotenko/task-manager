import styled from "styled-components";

export const WorkerWrapp = styled.div`
  display: inline-flex;
  align-items: center;

  border-radius: 24px;
  cursor: pointer;
  background: #f2f2f2;
  border: 1px dashed #bdbdbd;
  min-width: 99px;

  padding: 0 9px;
`;

export const WorkerName = styled.div`
  font-weight: 500;
  font-size: 13px;
  color: #333333;
`;
